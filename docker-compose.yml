version: '3'
services:
  server:
    build:
      dockerfile: Dockerfile
      context: .
      args:
        NODE_ENV: development
    env_file: .env
    entrypoint: ["yarn", "run", "start:dev"]
    ports:
      - "8000:80"